---
import Layout from '../../../layouts/Layout.astro';
---
<Layout title="AP Chemistry: Chemical Bonding Quiz" description="Test your knowledge of chemical bonding concepts including Lewis structures, molecular geometry, hybridization, and bond polarity at the AP Chemistry level.">
  <style>
    .quiz-header { background: #26215C; border-radius: 12px; padding: 2rem; margin-bottom: 2rem; }
    .quiz-title { font-size: 26px; font-weight: 600; color: #EEEDFE; margin-bottom: 8px; }
    .quiz-meta { font-size: 14px; color: #AFA9EC; }
    .question-box { background: white; border-radius: 12px; border: 0.5px solid #e0e0e0; padding: 2rem; margin-bottom: 1.5rem; display: none; }
    .question-box.active { display: block; }
    .question-number { font-size: 12px; font-weight: 600; color: #888; text-transform: uppercase; letter-spacing: 0.05em; margin-bottom: 8px; }
    .question-text { font-size: 18px; font-weight: 500; color: #1a1a2e; margin-bottom: 1.5rem; line-height: 1.5; }
    .options { display: flex; flex-direction: column; gap: 10px; }
    .option { background: #f5f5f5; border: 2px solid transparent; border-radius: 8px; padding: 12px 16px; font-size: 15px; color: #1a1a2e; cursor: pointer; text-align: left; font-family: inherit; }
    .option:hover:not(:disabled) { border-color: #AFA9EC; }
    .option.correct { background: #E1F5EE; border-color: #085041; color: #085041; }
    .option.wrong { background: #FAECE7; border-color: #712B13; color: #712B13; }
    .option.reveal { background: #E1F5EE; border-color: #085041; color: #085041; }
    .feedback { margin-top: 1rem; padding: 0.75rem 1rem; border-radius: 8px; font-size: 14px; display: none; line-height: 1.5; }
    .feedback.show { display: block; }
    .feedback.correct { background: #E1F5EE; color: #085041; }
    .feedback.wrong { background: #FAECE7; color: #712B13; }
    .next-btn { margin-top: 1rem; background: #534AB7; color: white; border: none; padding: 10px 24px; border-radius: 8px; font-size: 15px; cursor: pointer; font-family: inherit; display: none; }
    .next-btn.show { display: block; }
    .progress { background: #f5f5f5; border-radius: 999px; height: 8px; margin-bottom: 2rem; overflow: hidden; }
    .progress-bar { background: #534AB7; height: 100%; border-radius: 999px; transition: width 0.3s; }
    .score-box { background: #26215C; border-radius: 12px; padding: 2rem; text-align: center; display: none; }
    .score-box.show { display: block; }
    .score-title { font-size: 22px; font-weight: 600; color: #EEEDFE; margin-bottom: 8px; }
    .score-value { font-size: 48px; font-weight: 600; color: #FAC775; margin-bottom: 8px; }
    .score-message { font-size: 15px; color: #AFA9EC; margin-bottom: 1.5rem; }
    .retry-btn { background: #534AB7; color: white; border: none; padding: 10px 24px; border-radius: 8px; font-size: 15px; cursor: pointer; font-family: inherit; }
  </style>

  <div class="quiz-header">
    <h1 class="quiz-title">AP Chemistry: Chemical Bonding Quiz</h1>
    <div class="quiz-meta">5 questions · Multiple choice · Instant feedback</div>
  </div>

  <div class="progress"><div class="progress-bar" id="progress-bar" style="width:0%"></div></div>
  <div id="quiz-container"></div>
  <div class="score-box" id="score-box">
    <div class="score-title">Quiz complete!</div>
    <div class="score-value" id="score-value"></div>
    <div class="score-message" id="score-message"></div>
    <button class="retry-btn" id="retry-btn">Try again</button>
  </div>

  <script>
    const questions = [
      {
        text: "What is the molecular geometry of SF₄ according to VSEPR theory?",
        options: [
          "Tetrahedral",
          "Seesaw (distorted tetrahedral)",
          "Square planar",
          "Trigonal bipyramidal"
        ],
        correct: 1,
        feedback: {
          correct: "Correct! SF₄ has 5 electron domains (4 bonding, 1 lone pair) around sulfur, giving it a seesaw geometry due to the lone pair occupying an equatorial position.",
          wrong: "Incorrect. SF₄ has 5 electron domains with 1 lone pair, resulting in a seesaw geometry. The lone pair occupies an equatorial position in the trigonal bipyramidal electron geometry."
        }
      },
      {
        text: "Which molecule exhibits sp³d² hybridization at the central atom?",
        options: [
          "PCl₃",
          "SF₆",
          "BF₃",
          "CH₄"
        ],
        correct: 1,
        feedback: {
          correct: "Correct! SF₆ has 6 electron domains around sulfur, requiring sp³d² hybridization to accommodate the octahedral geometry.",
          wrong: "Incorrect. SF₆ requires sp³d² hybridization for its 6 electron domains. PCl₃ uses sp³, BF₃ uses sp², and CH₄ uses sp³ hybridization."
        }
      },
      {
        text: "Based on formal charge calculations, which Lewis structure for SO₂ is most reasonable?",
        options: [
          "S with two single bonds to O atoms, each O has 3 lone pairs",
          "S with one single bond and one double bond to O atoms",
          "S with two double bonds to O atoms",
          "S with two triple bonds to O atoms"
        ],
        correct: 1,
        feedback: {
          correct: "Correct! The structure with one single and one double bond minimizes formal charges, giving S a formal charge of +1, one O a formal charge of 0, and the other O a formal charge of -1.",
          wrong: "Incorrect. The most reasonable structure has one single and one double bond to minimize formal charges. Two single bonds would give sulfur a formal charge of +2."
        }
      },
      {
        text: "Which statement about the bonding in benzene (C₆H₆) is most accurate?",
        options: [
          "Benzene has alternating single and double bonds between carbon atoms",
          "All carbon atoms in benzene are sp² hybridized with delocalized π electrons",
          "Benzene contains only σ bonds between carbon atoms",
          "The carbon atoms in benzene exhibit sp³ hybridization"
        ],
        correct: 1,
        feedback: {
          correct: "Correct! All carbon atoms in benzene are sp² hybridized, forming σ bonds, while the remaining p orbitals overlap to create a delocalized π system above and below the ring.",
          wrong: "Incorrect. Benzene has delocalized π electrons, not discrete double bonds. All carbons are sp² hybridized, creating a resonance-stabilized aromatic system."
        }
      },
      {
        text: "Which intermolecular force is primarily responsible for the high boiling point of hydrogen fluoride (HF)?",
        options: [
          "London dispersion forces",
          "Dipole-dipole interactions",
          "Hydrogen bonding",
          "Ion-dipole interactions"
        ],
        correct: 2,
        feedback: {
          correct: "Correct! HF exhibits strong hydrogen bonding due to the highly electronegative fluorine atom bonded to hydrogen, resulting in a much higher boiling point than expected for such a small molecule.",
          wrong: "Incorrect. While HF does experience other intermolecular forces, hydrogen bonding is the dominant force due to the H-F bond, making HF's boiling point exceptionally high for its molecular size."
        }
      }
    ];

    let score = 0;
    let current = 0;
    let answered = false;

    function buildQuestion(index) {
      const q = questions[index];
      const div = document.createElement('div');
      div.className = 'question-box active';
      div.id = 'question-box';
      div.innerHTML = `
        <div class="question-number">Question ${index + 1} of ${questions.length}</div>
        <div class="question-text">${q.text}</div>
        <div class="options" id="options-box">
          ${q.options.map((opt, i) => `<button class="option" data-index="${i}">${opt}</button>`).join('')}
        </div>
        <div class="feedback" id="feedback-box"></div>
        <button class="next-btn" id="next-btn">${index === questions.length - 1 ? 'See my score →' : 'Next question →'}</button>
      `;
      return div;
    }

    function showQuestion(index) {
      answered = false;
      const container = document.getElementById('quiz-container');
      container.innerHTML = '';
      const qDiv = buildQuestion(index);
      container.appendChild(qDiv);
      document.getElementById('options-box').querySelectorAll('.option').forEach(btn => {
        btn.addEventListener('click', function() {
          if (answered) return;
          answered = true;
          const chosen = parseInt(this.dataset.index);
          const correct = questions[index].correct;
          const allOptions = document.getElementById('options-box').querySelectorAll('.option');
          allOptions.forEach(b => b.disabled = true);
          const fb = document.getElementById('feedback-box');
          if (chosen === correct) {
            this.classList.add('correct');
            fb.textContent = questions[index].feedback.correct;
            fb.className = 'feedback show correct';
            score++;
          } else {
            this.classList.add('wrong');
            allOptions[correct].classList.add('reveal');
            fb.textContent = questions[index].feedback.wrong;
            fb.className = 'feedback show wrong';
          }
          document.getElementById('next-btn').classList.add('show');
          document.getElementById('progress-bar').style.width = ((index + 1) / questions.length * 100) + '%';
        });
      });
      document.getElementById('next-btn').addEventListener('click', function() {
        if (index === questions.length - 1) { showScore(); } else { current++; showQuestion(current); }
      });
    }

    function showScore() {
      document.getElementById('quiz-container').innerHTML = '';
      const scoreBox = document.getElementById('score-box');
      scoreBox.classList.add('show');
      document.getElementById('score-value').textContent = score + '/' + questions.length;
      const pct = score / questions.length;
      let msg = pct === 1 ? 'Perfect score!' : pct